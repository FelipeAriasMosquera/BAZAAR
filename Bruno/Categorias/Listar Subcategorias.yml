info:
  name: Listar Subcategorias
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/categorias/4/subcategorias"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
