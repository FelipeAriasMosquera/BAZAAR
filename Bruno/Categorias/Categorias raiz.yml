info:
  name: Categorias raiz
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/categorias/raiz"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
