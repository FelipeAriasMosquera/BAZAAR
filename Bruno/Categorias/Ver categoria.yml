info:
  name: Ver categoria
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/categorias/4"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
